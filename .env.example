# ============================================
# ProxyHub Environment Configuration
# ============================================
# 
# ⚠️ SECURITY NOTICE: 
# - NEVER commit real secrets to version control
# - Generate strong random values for production
# - Rotate secrets regularly
# ============================================

# --------------------------------------------
# 1. Node Environment
# --------------------------------------------
NODE_ENV=development
# Options: development | production | test

# --------------------------------------------
# 2. Server Configuration
# --------------------------------------------
PORT=3000
API_PREFIX=/api/v1

# --------------------------------------------
# 3. Database Configuration (PostgreSQL)
# --------------------------------------------
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres123
DATABASE_NAME=proxyhub

# Database Sync Mode
# ⚠️ Set to false in production and use migrations instead
DATABASE_SYNC=false

# --------------------------------------------
# 4. Redis Configuration
# --------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password  # Uncomment if Redis has password
REDIS_DB=0

# --------------------------------------------
# 5. JWT Authentication Configuration
# --------------------------------------------
# ⚠️ CRITICAL: JWT_SECRET must be at least 32 characters long
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARACTERS_LONG
JWT_EXPIRES_IN=2h
JWT_REFRESH_EXPIRES_IN=7d

# --------------------------------------------
# 6. 985Proxy API Configuration
# --------------------------------------------
# ⚠️ REQUIRED: Obtain from 985Proxy dashboard
# API Key format: ne_xxxxxxxx-base64EncodedString==
PROXY_985_API_KEY=your_985proxy_api_key_here
PROXY_985_BASE_URL=https://open-api.985proxy.com
PROXY_985_ZONE=your_zone_id_here

# Test Mode (false = production, charges real money)
PROXY_985_TEST_MODE=false

# --------------------------------------------
# 7. Email Service Configuration (SMTP)
# --------------------------------------------
# Primary Email Service (Outlook example)
MAIL_HOST=smtp.office365.com
MAIL_PORT=587
MAIL_USER=your-email@outlook.com
MAIL_PASSWORD=your_app_password_here
MAIL_FROM=ProxyHub <noreply@proxyhub.com>

# Backup Email Service (Gmail example)
# ⚠️ Gmail requires "App Password" - see docs/GMAIL-SMTP-SETUP.md
MAIL_HOST_BACKUP=smtp.gmail.com
MAIL_PORT_BACKUP=587
MAIL_USER_BACKUP=your-backup@gmail.com
MAIL_PASSWORD_BACKUP=your_gmail_app_password

# --------------------------------------------
# 8. Telegram Bot Configuration (Optional)
# --------------------------------------------
# TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# TELEGRAM_BOT_USERNAME=YourBot_bot
# 
# ⚠️ Comment out if not using Telegram notifications

# --------------------------------------------
# 9. Frontend Configuration
# --------------------------------------------
FRONTEND_URL=http://localhost:8080

# --------------------------------------------
# 10. CORS Configuration
# --------------------------------------------
# Development: CORS is auto-configured for localhost
# Production: Provide comma-separated list of allowed origins
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# 
# ⚠️ REQUIRED in production to prevent unauthorized access

# --------------------------------------------
# 11. Logging Configuration
# --------------------------------------------
LOG_LEVEL=info
# Options: error | warn | info | debug | verbose

# ============================================
# Security Checklist:
# ============================================
# [ ] JWT_SECRET is at least 32 characters and randomly generated
# [ ] All default passwords are changed
# [ ] PROXY_985_API_KEY is set with valid API key
# [ ] PROXY_985_ZONE is set with your zone ID
# [ ] Email credentials are valid
# [ ] CORS_ORIGINS is configured for production
# [ ] DATABASE_SYNC is set to false for production
# [ ] All sensitive values are stored securely (not in git)
# ============================================

