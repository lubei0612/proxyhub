# ProxyHub 快速启动指南

## 🚀 一键启动（推荐）

### Windows 用户

**双击运行**: `启动ProxyHub.bat`

该脚本将自动：
1. 启动数据库服务（PostgreSQL + Redis）
2. 启动后端服务（NestJS）
3. 启动前端服务（Vue 3）
4. 自动打开浏览器

---

## 📋 前置要求

### 必须安装
- ✅ **Docker Desktop** (用于数据库)
- ✅ **Node.js v18+** (用于前后端)
- ✅ **npm** (Node.js自带)

### 可选安装
- Git (用于代码版本控制)

---

## 🔧 手动启动（分步）

如果一键启动遇到问题，可以按顺序手动启动各服务：

### 第 1 步：启动数据库
```bash
双击运行: start-database.bat
```
等待15秒，确保数据库完全启动。

### 第 2 步：启动后端
```bash
双击运行: start-backend.bat
```
等待30秒，等待后端编译并启动。

### 第 3 步：启动前端
```bash
双击运行: start-frontend.bat
```
等待20秒，前端开发服务器启动。

---

## 🌐 访问地址

启动成功后，访问以下地址：

- **前端页面**: http://localhost:8080 （或 8081）
- **后端API**: http://localhost:3000
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

---

## 👤 测试账号

### 管理员账号
- 邮箱: `admin@admin.com`
- 密码: `admin123456`

### 普通用户账号
- 邮箱: `test@test.com`
- 密码: `test123456`

---

## ❓ 常见问题

### Q1: PowerShell 执行策略错误
**错误信息**: "无法加载文件...因为在此系统上禁止运行脚本"

**解决方案**: 使用提供的 `.bat` 批处理文件启动，它们不受PowerShell执行策略限制。

### Q2: 前端页面无法访问
**可能原因**: 端口8080被占用，Vite自动切换到8081

**解决方案**: 尝试访问 http://localhost:8081

### Q3: Docker启动失败
**解决方案**: 
1. 确保Docker Desktop已启动
2. 打开Docker Desktop，等待状态变为"Running"
3. 重新运行 `start-database.bat`

### Q4: 后端启动失败
**可能原因**: 端口3000被占用或数据库未就绪

**解决方案**:
1. 确保数据库已启动（先运行 start-database.bat）
2. 检查3000端口是否被占用：`netstat -ano | findstr :3000`
3. 如果被占用，结束占用进程或修改后端端口

### Q5: npm install 失败
**解决方案**:
```bash
# 清除缓存
cd backend
npm cache clean --force
npm install

cd ../frontend
npm cache clean --force
npm install
```

---

## 🛑 停止服务

**方法1**: 直接关闭各服务窗口

**方法2**: 使用Ctrl+C在各窗口中停止服务

**停止数据库**:
```bash
docker-compose -f docker-compose.dev.yml down
```

---

## 🔄 重启服务

1. 停止所有服务（关闭窗口或Ctrl+C）
2. 重新双击运行 `启动ProxyHub.bat`

---

## 📞 获取帮助

如遇到其他问题，请：
1. 检查各服务窗口的错误信息
2. 查看项目 README.md
3. 提交 GitHub Issue

---

## ⚡ 快速命令参考

```bash
# 启动所有服务
双击: 启动ProxyHub.bat

# 仅启动数据库
双击: start-database.bat

# 仅启动后端
双击: start-backend.bat

# 仅启动前端
双击: start-frontend.bat

# 停止数据库
docker-compose -f docker-compose.dev.yml down

# 查看端口占用
netstat -ano | findstr :3000  # 后端
netstat -ano | findstr :8080  # 前端
netstat -ano | findstr :5432  # PostgreSQL
```

---

**祝您使用愉快！** 🎉

