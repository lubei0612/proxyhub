# ProxyHub 移动端响应式使用指南

**版本**: v1.0.0  
**文档生成时间**: 2025-11-03  
**支持设备**: 手机、平板、桌面

---

## 📱 断点定义

| 断点 | 屏幕宽度 | 设备类型 | 列数 |
|------|----------|----------|------|
| XS | < 576px | 手机（竖屏） | 1列 |
| SM | 576px - 768px | 小屏手机/手机横屏 | 1-2列 |
| MD | 768px - 992px | 平板 | 2-3列 |
| LG | 992px - 1200px | 小屏电脑 | 3-4列 |
| XL | > 1200px | 大屏电脑 | 4列 |

---

## 🎨 响应式工具类

### 1. 显示/隐藏

```vue
<!-- 移动端隐藏 -->
<div class="mobile-hidden">仅在桌面端显示</div>

<!-- 桌面端隐藏 -->
<div class="desktop-hidden">仅在移动端显示</div>
```

### 2. 容器

```vue
<!-- 响应式容器（自动padding） -->
<div class="container-responsive">
  内容自动适配各种屏幕
</div>

<!-- 响应式卡片（自动padding） -->
<el-card class="card-responsive">
  卡片内边距自动适配
</el-card>
```

### 3. 布局

```vue
<!-- 响应式Grid布局 -->
<div class="grid-responsive">
  <div>卡片1</div>
  <div>卡片2</div>
  <div>卡片3</div>
  <div>卡片4</div>
</div>
<!-- 桌面：4列，平板：2列，手机：1列 -->

<!-- 响应式Flex布局 -->
<div class="flex-responsive">
  <div>左侧</div>
  <div>右侧</div>
</div>
<!-- 桌面：水平排列，手机：垂直排列 -->
```

### 4. 表格

```vue
<!-- 响应式表格（移动端横向滚动） -->
<div class="table-responsive">
  <el-table :data="tableData">
    <el-table-column label="列1" />
    <el-table-column label="列2" />
  </el-table>
</div>
```

### 5. 表单

```vue
<!-- 响应式表单 -->
<el-form class="form-responsive" :model="form" label-width="120px">
  <el-form-item label="姓名">
    <el-input v-model="form.name" />
  </el-form-item>
</el-form>
<!-- 桌面：label在左侧，手机：label在上方 -->
```

### 6. 按钮组

```vue
<!-- 响应式按钮组 -->
<div class="button-group-responsive">
  <el-button type="primary">按钮1</el-button>
  <el-button>按钮2</el-button>
  <el-button>按钮3</el-button>
</div>
<!-- 桌面：水平排列，手机：垂直排列，全宽 -->
```

### 7. 筛选区域

```vue
<!-- 响应式筛选 -->
<div class="filter-section-responsive">
  <el-row :gutter="15">
    <el-col :span="6">
      <el-input placeholder="搜索" />
    </el-col>
    <el-col :span="6">
      <el-select placeholder="选择" />
    </el-col>
  </el-row>
</div>
<!-- 桌面：一行4个，手机：每行1个 -->
```

### 8. 分页

```vue
<!-- 响应式分页 -->
<div class="pagination-responsive">
  <el-pagination
    :current-page="1"
    :total="100"
    layout="total, sizes, prev, pager, next, jumper"
  />
</div>
<!-- 手机端：隐藏sizes和jumper，居中显示 -->
```

---

## 🛠️ SCSS Mixins使用

### 在Vue组件中使用

```vue
<style scoped lang="scss">
@import '@/styles/responsive.scss';

.my-component {
  padding: 20px;
  
  // 手机端 (< 576px)
  @include xs {
    padding: 10px;
    font-size: 12px;
  }
  
  // 小屏手机 (< 768px)
  @include sm {
    padding: 15px;
    flex-direction: column;
  }
  
  // 平板 (< 992px)
  @include md {
    padding: 18px;
  }
  
  // 小屏电脑 (< 1200px)
  @include lg {
    padding: 20px;
  }
}
</style>
```

---

## 📋 最佳实践

### 1. 仪表盘页面

```vue
<template>
  <div class="dashboard-container container-responsive">
    <h1 class="text-responsive">仪表盘</h1>
    
    <!-- 统计卡片 - 响应式Grid -->
    <div class="grid-responsive">
      <el-card class="stat-card-responsive">
        <div class="stat-label">总用户数</div>
        <div class="stat-value">1,234</div>
      </el-card>
      <!-- 更多卡片... -->
    </div>
    
    <!-- 图表 - 响应式Flex -->
    <div class="flex-responsive">
      <el-card class="card-responsive" style="flex: 1">
        <!-- 条形图 -->
      </el-card>
      <el-card class="card-responsive" style="flex: 1">
        <!-- 饼图 -->
      </el-card>
    </div>
  </div>
</template>

<style scoped lang="scss">
@import '@/styles/responsive.scss';

.dashboard-container {
  padding: 24px;
  
  @include sm {
    padding: 15px;
  }
}

.stat-card-responsive {
  text-align: center;
  
  .stat-label {
    font-size: 14px;
    color: #909399;
  }
  
  .stat-value {
    font-size: 32px;
    font-weight: 600;
    margin-top: 8px;
    
    @include sm {
      font-size: 24px;
    }
  }
}
</style>
```

### 2. 列表页面

```vue
<template>
  <div class="list-container container-responsive">
    <!-- 筛选区 -->
    <el-card class="card-responsive">
      <div class="filter-section-responsive">
        <el-row :gutter="15">
          <el-col :span="6">
            <el-input placeholder="搜索" clearable />
          </el-col>
          <el-col :span="6">
            <el-select placeholder="状态" clearable />
          </el-col>
          <el-col :span="6">
            <el-date-picker type="daterange" />
          </el-col>
          <el-col :span="6">
            <div class="button-group-responsive">
              <el-button type="primary">搜索</el-button>
              <el-button>重置</el-button>
            </div>
          </el-col>
        </el-row>
      </div>
    </el-card>
    
    <!-- 表格 -->
    <el-card class="card-responsive">
      <div class="table-responsive">
        <el-table :data="tableData">
          <el-table-column label="ID" prop="id" />
          <el-table-column label="名称" prop="name" />
          <el-table-column label="状态" prop="status" />
        </el-table>
      </div>
      
      <!-- 分页 -->
      <div class="pagination-responsive">
        <el-pagination :total="100" />
      </div>
    </el-card>
  </div>
</template>
```

### 3. 表单页面

```vue
<template>
  <div class="form-container container-responsive">
    <el-card class="card-responsive">
      <el-form class="form-responsive" :model="form" label-width="120px">
        <el-form-item label="用户名">
          <el-input v-model="form.username" />
        </el-form-item>
        
        <el-form-item label="邮箱">
          <el-input v-model="form.email" type="email" />
        </el-form-item>
        
        <el-form-item>
          <div class="button-group-responsive">
            <el-button type="primary">提交</el-button>
            <el-button>取消</el-button>
          </div>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>
```

### 4. 详情页面

```vue
<template>
  <div class="detail-container container-responsive">
    <el-card class="card-responsive">
      <el-descriptions border :column="descColumn">
        <el-descriptions-item label="ID">123</el-descriptions-item>
        <el-descriptions-item label="名称">ProxyHub</el-descriptions-item>
        <el-descriptions-item label="状态">正常</el-descriptions-item>
      </el-descriptions>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue';

// 响应式列数
const descColumn = computed(() => {
  const width = window.innerWidth;
  if (width < 768) return 1;  // 手机：1列
  if (width < 992) return 2;  // 平板：2列
  return 3;  // 桌面：3列
});
</script>
```

---

## 📐 布局响应式

### DashboardLayout响应式（已实现）

```scss
// 侧边栏响应式
.sidebar {
  width: 200px;
  
  @media (max-width: 992px) {
    width: 64px;  // 仅显示图标
  }
  
  @media (max-width: 768px) {
    position: fixed;
    z-index: 1000;
    transform: translateX(-100%);
    
    &.show {
      transform: translateX(0);
    }
  }
}

// Header响应式
.header {
  .welcome {
    @media (max-width: 576px) {
      display: none;
    }
  }
  
  .balance {
    @media (max-width: 768px) {
      display: none;
    }
  }
}
```

---

## 🎯 测试指南

### Chrome DevTools测试

1. 打开Chrome DevTools（F12）
2. 点击设备工具栏图标（Ctrl+Shift+M）
3. 选择设备：
   - iPhone SE (375x667)
   - iPhone 12 Pro (390x844)
   - iPad (768x1024)
   - iPad Pro (1024x1366)

### 响应式断点测试

| 断点 | 宽度 | 测试重点 |
|------|------|----------|
| XS | 375px | 布局是否垂直排列，文字是否可读 |
| SM | 576px | 按钮是否全宽，表单是否垂直 |
| MD | 768px | 表格是否滚动，Grid是否2列 |
| LG | 992px | 侧边栏是否折叠，Grid是否3列 |
| XL | 1200px | 所有功能是否正常显示 |

### 功能测试清单

- [ ] 登录页面在手机端可用
- [ ] 仪表盘在手机端正常显示
- [ ] 表格在手机端可横向滚动
- [ ] 筛选条件在手机端垂直排列
- [ ] 按钮在手机端全宽显示
- [ ] 表单在手机端label在上方
- [ ] 分页在手机端简化显示
- [ ] Dialog在手机端适应宽度
- [ ] 侧边栏在手机端可折叠
- [ ] 导航菜单在手机端可用

---

## ⚙️ 配置说明

### viewport设置

`index.html`中确保有以下meta标签：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

### Element Plus响应式

Element Plus的Grid系统自动响应式：

```vue
<el-row :gutter="20">
  <el-col :xs="24" :sm="12" :md="8" :lg="6" :xl="4">
    内容
  </el-col>
</el-row>
```

---

## 🚀 性能优化

### 1. 图片响应式

```vue
<img 
  :src="imageSrc"
  :srcset="`${imageSrc} 1x, ${imageSrc2x} 2x`"
  alt="responsive image"
/>
```

### 2. 条件渲染

```vue
<template>
  <div v-if="isMobile">
    <!-- 移动端组件 -->
  </div>
  <div v-else>
    <!-- 桌面端组件 -->
  </div>
</template>

<script setup>
const isMobile = computed(() => window.innerWidth < 768);
</script>
```

### 3. 延迟加载

```vue
<template>
  <component :is="layoutComponent" />
</template>

<script setup>
import { computed, defineAsyncComponent } from 'vue';

const layoutComponent = computed(() => {
  return window.innerWidth < 768
    ? defineAsyncComponent(() => import('./MobileLayout.vue'))
    : defineAsyncComponent(() => import('./DesktopLayout.vue'));
});
</script>
```

---

## 📚 参考资料

- [Element Plus 响应式布局](https://element-plus.org/zh-CN/component/layout.html)
- [MDN 响应式设计](https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Responsive_Design)
- [Chrome DevTools 设备模拟](https://developer.chrome.com/docs/devtools/device-mode/)

---

**文档版本**: v1.0.0  
**最后更新**: 2025-11-03  
**维护团队**: ProxyHub Dev Team

